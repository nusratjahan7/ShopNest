import express from "express";import cors from "cors";import pinoHttp from "pino-http";import {env} from "./config/env.js";import {apiRateLimiter} from "./middlewares/rate-limit.middleware.js";import {notFoundMiddleware} from "./middlewares/not-found.middleware.js";import {errorMiddleware} from "./middlewares/error.middleware.js";import routes from "./routes/index.js";import {auth} from "./modules/auth/auth.js";export const app=express();app.use(cors({origin:env.FRONTEND_URL,credentials:true}));app.use(express.json({limit:"1mb"}));app.use(pinoHttp());app.use(apiRateLimiter);app.get("/health",(_req,res)=>res.json({success:true,status:"healthy",service:"shopnest-backend"}));app.all("/api/auth/*splat",async(req,res)=>{const response=await auth.handler(req);res.status(response.status);response.headers.forEach((v,k)=>res.setHeader(k,v));res.send(await response.text())});app.use("/api",routes);app.use(notFoundMiddleware);app.use(errorMiddleware);
